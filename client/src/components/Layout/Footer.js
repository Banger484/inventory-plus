import './Footer.js'

export default function Footer () {
    return (
        <footer>
            <button>Back</button>
            <button>Home</button>
            <button>Logout</button>
        </footer>
    )
}